exports.capitalize = function kevo(string){


	return string.toLowerCase().replace( /\b\w/g, function (m) {
            return m.toUpperCase();
        });
	
	return string.charAt(0).toUpperCase() + string.slice(1);
	return string;
	return 'duuude';
}